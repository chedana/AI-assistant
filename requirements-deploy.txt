# Deployment requirements (Render free tier, no PyTorch)
# Uses fastembed (ONNX runtime) instead of sentence-transformers + PyTorch

# Web framework
fastapi
uvicorn[standard]
python-multipart

# Async / HTTP
httpx
aiohttp
anyio

# LLM
openai

# Vector DB
qdrant-client

# Embedding model (ONNX — no PyTorch needed)
# Pinned: newer versions pull in py-rust-stemmers which requires Rust compilation
fastembed==0.3.6

# Graph orchestration
langgraph

# Data
numpy
pandas
pyarrow
scikit-learn
scipy

# Utilities
cachetools
pydantic
python-dateutil
regex
requests
tqdm
tiktoken
tenacity
joblib
pytz
loguru
